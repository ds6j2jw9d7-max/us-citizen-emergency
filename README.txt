WEB APP — VERSION 1.0
Last verified: August 20, 2026.

This folder contains the static HTTPS-ready web app. To update the existing GitHub Pages site,
replace the files in the repository root with the files in this folder. Keep index.html at the
repository root.

The site is designed for Safari → Share → Add to Home Screen and supports offline caching after
the first successful load.
